<PreferenceScreen xmlns:android = "http://schemas.android.com/apk/res/android">
    
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue = "640x480-1000kbps"
        android:entries = "@array/sp_video_resolution_titles"
        android:entryValues = "@array/sp_video_resolution_values"
        android:key = "video_resolution"
        android:negativeButtonText = "@null"
        android:positiveButtonText = "@null"
        android:title = "@string/sp_title_video_resolution" />
    
    <ListPreference
        android:defaultValue = "H264"
        android:entries = "@array/list_preference_entries"
        android:entryValues = "@array/list_preference_entry_values"
        android:key = "video_codec"
        android:negativeButtonText = "@null"
        android:positiveButtonText = "@null"
        android:title = "@string/sp_title_video_codec_setting" />
    
    <!-- This preference simply launches an intent when selected. Use this UI sparingly, per
         design guidelines. -->

</PreferenceScreen>
